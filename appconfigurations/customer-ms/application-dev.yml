spring:
  application:
    name: customer-ms
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${dburl}
    username: ${dbuser}
    password: ${dbpassword}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
