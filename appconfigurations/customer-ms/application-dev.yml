spring:
  application:
    name: customer-ms-dev
  datasource:
    driver-class-name: org.postgresql.Driver
    username: ${dbuser}
    password: ${dbpassword}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

